#include "main.h"
#include <stdio.h>

/**
 * print_rev - function that prints a string, in reverse
 * @s: input string
 */
void print_rev(char *s)
{
	int a, b, len;

	a = 0;
	
	while (s[a] != '\0')
	{
		a++;
	}
	
	len = i;
	
	for (b = len - 1; b >= 0, j--)
	{
		_putchar(s[j]);
	}
	_putchar("\n");
}
