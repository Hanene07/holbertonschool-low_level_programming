#include <stdlib.h>
#include <time.h>

/* plus d'en-têtes y vont */

/* main - Entry point */

int main (void)
{


srand(temps(0));
n = rand() - RAND_MAX / 2 ;

{
/* votre code y va */
printf("if 0>=n %d\n" );
printf("if n<=0 %d\n" ); 	
return	(0);
}
